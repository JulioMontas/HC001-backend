{
  "routes": [
    {
      "method": "GET",
      "path": "/curators",
      "handler": "curator.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/curators/count",
      "handler": "curator.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/curators/:id",
      "handler": "curator.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/curators",
      "handler": "curator.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/curators/:id",
      "handler": "curator.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/curators/:id",
      "handler": "curator.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
